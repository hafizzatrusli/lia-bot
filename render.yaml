services:
  - type: web
    name: lia-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn telegram_bot:app --bind 0.0.0.0:8080
    envVars:
      - key: PUBLIC_URL
        value: https://lia-bot.onrender.com
